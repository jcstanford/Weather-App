name: weather-app
region: nyc
services:
- environment_slug: html
  github:
    branch: main
    deploy_on_push: true
    repo: jcstanford/weather-app
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
  name: weather-app
  routes:
  - path: /
  source_dir: /sourcecode